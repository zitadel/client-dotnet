// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech
#nullable enable

using System.Text.Json;
using System.Text.Json.Serialization;

namespace Zitadel.Client.Models;

[JsonConverter(typeof(UserServiceGenderConverter))]
public enum UserServiceGender
{
    GENDERUNSPECIFIED,

    GENDERFEMALE,

    GENDERMALE,

    GENDERDIVERSE,
}

/// <summary>
/// Serializes <see cref="UserServiceGender"/> to and from its OpenAPI wire string
/// values. This generated converter replaces the .NET 9+
/// <c>[JsonStringEnumMemberName]</c> attribute so the client targets net8.0
/// (LTS); unknown values throw a <see cref="JsonException"/>, matching the
/// strict behaviour of the framework string-enum converter.
/// </summary>
internal sealed class UserServiceGenderConverter : JsonConverter<UserServiceGender>
{
    public override UserServiceGender Read(
        ref Utf8JsonReader reader,
        Type typeToConvert,
        JsonSerializerOptions options
    )
    {
        if (reader.TokenType != JsonTokenType.String)
        {
            throw new JsonException(
                $"Expected a JSON string for enum UserServiceGender but got {reader.TokenType}."
            );
        }

        string? value = reader.GetString();
        return value switch
        {
            "GENDER_UNSPECIFIED" => UserServiceGender.GENDERUNSPECIFIED,
            "GENDER_FEMALE" => UserServiceGender.GENDERFEMALE,
            "GENDER_MALE" => UserServiceGender.GENDERMALE,
            "GENDER_DIVERSE" => UserServiceGender.GENDERDIVERSE,
            _ => throw new JsonException($"Unknown value '{value}' for enum UserServiceGender."),
        };
    }

    public override void Write(
        Utf8JsonWriter writer,
        UserServiceGender value,
        JsonSerializerOptions options
    )
    {
        writer.WriteStringValue(
            value switch
            {
                UserServiceGender.GENDERUNSPECIFIED => "GENDER_UNSPECIFIED",
                UserServiceGender.GENDERFEMALE => "GENDER_FEMALE",
                UserServiceGender.GENDERMALE => "GENDER_MALE",
                UserServiceGender.GENDERDIVERSE => "GENDER_DIVERSE",
                _ => throw new JsonException(
                    $"Unknown value '{value}' for enum UserServiceGender."
                ),
            }
        );
    }
}
