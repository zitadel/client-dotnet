// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech
#nullable enable

using System.Text.Json;
using System.Text.Json.Serialization;

namespace Zitadel.Client.Models;

[JsonConverter(typeof(WebKeyServiceRSAHasherConverter))]
public enum WebKeyServiceRSAHasher
{
    RSAHASHERUNSPECIFIED,

    RSAHASHERSHA256,

    RSAHASHERSHA384,

    RSAHASHERSHA512,
}

/// <summary>
/// Serializes <see cref="WebKeyServiceRSAHasher"/> to and from its OpenAPI wire string
/// values. This generated converter replaces the .NET 9+
/// <c>[JsonStringEnumMemberName]</c> attribute so the client targets net8.0
/// (LTS); unknown values throw a <see cref="JsonException"/>, matching the
/// strict behaviour of the framework string-enum converter.
/// </summary>
internal sealed class WebKeyServiceRSAHasherConverter : JsonConverter<WebKeyServiceRSAHasher>
{
    public override WebKeyServiceRSAHasher Read(
        ref Utf8JsonReader reader,
        Type typeToConvert,
        JsonSerializerOptions options
    )
    {
        if (reader.TokenType != JsonTokenType.String)
        {
            throw new JsonException(
                $"Expected a JSON string for enum WebKeyServiceRSAHasher but got {reader.TokenType}."
            );
        }

        string? value = reader.GetString();
        return value switch
        {
            "RSA_HASHER_UNSPECIFIED" => WebKeyServiceRSAHasher.RSAHASHERUNSPECIFIED,
            "RSA_HASHER_SHA256" => WebKeyServiceRSAHasher.RSAHASHERSHA256,
            "RSA_HASHER_SHA384" => WebKeyServiceRSAHasher.RSAHASHERSHA384,
            "RSA_HASHER_SHA512" => WebKeyServiceRSAHasher.RSAHASHERSHA512,
            _ => throw new JsonException(
                $"Unknown value '{value}' for enum WebKeyServiceRSAHasher."
            ),
        };
    }

    public override void Write(
        Utf8JsonWriter writer,
        WebKeyServiceRSAHasher value,
        JsonSerializerOptions options
    )
    {
        writer.WriteStringValue(
            value switch
            {
                WebKeyServiceRSAHasher.RSAHASHERUNSPECIFIED => "RSA_HASHER_UNSPECIFIED",
                WebKeyServiceRSAHasher.RSAHASHERSHA256 => "RSA_HASHER_SHA256",
                WebKeyServiceRSAHasher.RSAHASHERSHA384 => "RSA_HASHER_SHA384",
                WebKeyServiceRSAHasher.RSAHASHERSHA512 => "RSA_HASHER_SHA512",
                _ => throw new JsonException(
                    $"Unknown value '{value}' for enum WebKeyServiceRSAHasher."
                ),
            }
        );
    }
}
