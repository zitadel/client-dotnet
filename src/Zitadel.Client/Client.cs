// Zitadel SDK
// The Zitadel SDK is a convenience wrapper around the Zitadel APIs to assist you in integrating with your Zitadel environment. This SDK enables you to handle resources, settings, and configurations within the Zitadel platform.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

using Zitadel.Client.Api;
using Zitadel.Client.Auth;

namespace Zitadel.Client;

/// <summary>
/// Unified entry point for all API services.
///
/// Takes an <see cref="IAuthenticator"/> and optionally <see cref="TransportOptions"/>,
/// then exposes each API group as a typed property. If the authenticator
/// implements <see cref="IHttpAwareAuthenticator"/>, the shared <see cref="IApiClient"/>
/// is injected so that authentication HTTP calls (token exchange, discovery)
/// use the same transport configuration as regular API calls.
///
/// Usage:
/// <code>
/// // Default transport
/// var client = new Client(authenticator);
///
/// // Custom transport (proxy, timeouts, etc.)
/// var transport = TransportOptions.Builder()
///     .Proxy("http://proxy:3128")
///     .Timeout(5000)
///     .Build();
/// var client = new Client(authenticator, transport);
/// </code>
/// </summary>
public sealed class Client : IDisposable
{
    private readonly DefaultApiClient _apiClient;

    /// <summary>API operations for the ActionServiceApi group.</summary>
    public ActionServiceApi ActionService { get; }

    /// <summary>API operations for the ApplicationServiceApi group.</summary>
    public ApplicationServiceApi ApplicationService { get; }

    /// <summary>API operations for the AuthorizationServiceApi group.</summary>
    public AuthorizationServiceApi AuthorizationService { get; }

    /// <summary>API operations for the BetaActionServiceApi group.</summary>
    public BetaActionServiceApi BetaActionService { get; }

    /// <summary>API operations for the BetaAppServiceApi group.</summary>
    public BetaAppServiceApi BetaAppService { get; }

    /// <summary>API operations for the BetaAuthorizationServiceApi group.</summary>
    public BetaAuthorizationServiceApi BetaAuthorizationService { get; }

    /// <summary>API operations for the BetaFeatureServiceApi group.</summary>
    public BetaFeatureServiceApi BetaFeatureService { get; }

    /// <summary>API operations for the BetaInstanceServiceApi group.</summary>
    public BetaInstanceServiceApi BetaInstanceService { get; }

    /// <summary>API operations for the BetaInternalPermissionServiceApi group.</summary>
    public BetaInternalPermissionServiceApi BetaInternalPermissionService { get; }

    /// <summary>API operations for the BetaOIDCServiceApi group.</summary>
    public BetaOIDCServiceApi BetaOIDCService { get; }

    /// <summary>API operations for the BetaOrganizationServiceApi group.</summary>
    public BetaOrganizationServiceApi BetaOrganizationService { get; }

    /// <summary>API operations for the BetaProjectServiceApi group.</summary>
    public BetaProjectServiceApi BetaProjectService { get; }

    /// <summary>API operations for the BetaSessionServiceApi group.</summary>
    public BetaSessionServiceApi BetaSessionService { get; }

    /// <summary>API operations for the BetaSettingsServiceApi group.</summary>
    public BetaSettingsServiceApi BetaSettingsService { get; }

    /// <summary>API operations for the BetaTelemetryServiceApi group.</summary>
    public BetaTelemetryServiceApi BetaTelemetryService { get; }

    /// <summary>API operations for the BetaUserServiceApi group.</summary>
    public BetaUserServiceApi BetaUserService { get; }

    /// <summary>API operations for the BetaWebKeyServiceApi group.</summary>
    public BetaWebKeyServiceApi BetaWebKeyService { get; }

    /// <summary>API operations for the FeatureServiceApi group.</summary>
    public FeatureServiceApi FeatureService { get; }

    /// <summary>API operations for the IdentityProviderServiceApi group.</summary>
    public IdentityProviderServiceApi IdentityProviderService { get; }

    /// <summary>API operations for the InstanceServiceApi group.</summary>
    public InstanceServiceApi InstanceService { get; }

    /// <summary>API operations for the InternalPermissionServiceApi group.</summary>
    public InternalPermissionServiceApi InternalPermissionService { get; }

    /// <summary>API operations for the OIDCServiceApi group.</summary>
    public OIDCServiceApi OIDCService { get; }

    /// <summary>API operations for the OrganizationServiceApi group.</summary>
    public OrganizationServiceApi OrganizationService { get; }

    /// <summary>API operations for the ProjectServiceApi group.</summary>
    public ProjectServiceApi ProjectService { get; }

    /// <summary>API operations for the SAMLServiceApi group.</summary>
    public SAMLServiceApi SAMLService { get; }

    /// <summary>API operations for the SessionServiceApi group.</summary>
    public SessionServiceApi SessionService { get; }

    /// <summary>API operations for the SettingsServiceApi group.</summary>
    public SettingsServiceApi SettingsService { get; }

    /// <summary>API operations for the UserServiceApi group.</summary>
    public UserServiceApi UserService { get; }

    /// <summary>API operations for the WebKeyServiceApi group.</summary>
    public WebKeyServiceApi WebKeyService { get; }

    /// <summary>
    /// Creates a new client with the given authenticator and default transport settings.
    /// </summary>
    /// <param name="authenticator">Provides host URL and auth credentials.</param>
    public Client(IAuthenticator authenticator)
        : this(authenticator, TransportOptions.Builder().Build()) { }

    /// <summary>
    /// Creates a new client with the given authenticator and transport options.
    ///
    /// If the authenticator implements <see cref="IHttpAwareAuthenticator"/>, the
    /// shared <see cref="IApiClient"/> is injected so that token exchange and
    /// discovery requests use the same proxy, TLS, and timeout settings.
    /// </summary>
    /// <param name="authenticator">Provides host URL and auth credentials.</param>
    /// <param name="transportOptions">HTTP transport configuration (proxy, TLS, timeouts, etc.).</param>
    public Client(IAuthenticator authenticator, TransportOptions? transportOptions)
    {
        ArgumentNullException.ThrowIfNull(authenticator);
        transportOptions ??= TransportOptions.Builder().Build();

        _apiClient = new DefaultApiClient(transportOptions);

        if (authenticator is IHttpAwareAuthenticator httpAware)
        {
            httpAware.SetApiClient(_apiClient);
        }

        Configuration config = Configuration.Builder().BaseUrl(authenticator.GetHost()).Build();
        ActionService = new ActionServiceApi(_apiClient, config, authenticator);
        ApplicationService = new ApplicationServiceApi(_apiClient, config, authenticator);
        AuthorizationService = new AuthorizationServiceApi(_apiClient, config, authenticator);
        BetaActionService = new BetaActionServiceApi(_apiClient, config, authenticator);
        BetaAppService = new BetaAppServiceApi(_apiClient, config, authenticator);
        BetaAuthorizationService = new BetaAuthorizationServiceApi(
            _apiClient,
            config,
            authenticator
        );
        BetaFeatureService = new BetaFeatureServiceApi(_apiClient, config, authenticator);
        BetaInstanceService = new BetaInstanceServiceApi(_apiClient, config, authenticator);
        BetaInternalPermissionService = new BetaInternalPermissionServiceApi(
            _apiClient,
            config,
            authenticator
        );
        BetaOIDCService = new BetaOIDCServiceApi(_apiClient, config, authenticator);
        BetaOrganizationService = new BetaOrganizationServiceApi(_apiClient, config, authenticator);
        BetaProjectService = new BetaProjectServiceApi(_apiClient, config, authenticator);
        BetaSessionService = new BetaSessionServiceApi(_apiClient, config, authenticator);
        BetaSettingsService = new BetaSettingsServiceApi(_apiClient, config, authenticator);
        BetaTelemetryService = new BetaTelemetryServiceApi(_apiClient, config, authenticator);
        BetaUserService = new BetaUserServiceApi(_apiClient, config, authenticator);
        BetaWebKeyService = new BetaWebKeyServiceApi(_apiClient, config, authenticator);
        FeatureService = new FeatureServiceApi(_apiClient, config, authenticator);
        IdentityProviderService = new IdentityProviderServiceApi(_apiClient, config, authenticator);
        InstanceService = new InstanceServiceApi(_apiClient, config, authenticator);
        InternalPermissionService = new InternalPermissionServiceApi(
            _apiClient,
            config,
            authenticator
        );
        OIDCService = new OIDCServiceApi(_apiClient, config, authenticator);
        OrganizationService = new OrganizationServiceApi(_apiClient, config, authenticator);
        ProjectService = new ProjectServiceApi(_apiClient, config, authenticator);
        SAMLService = new SAMLServiceApi(_apiClient, config, authenticator);
        SessionService = new SessionServiceApi(_apiClient, config, authenticator);
        SettingsService = new SettingsServiceApi(_apiClient, config, authenticator);
        UserService = new UserServiceApi(_apiClient, config, authenticator);
        WebKeyService = new WebKeyServiceApi(_apiClient, config, authenticator);
    }

    /// <summary>
    /// Creates a client authenticated with a static Bearer token.
    /// </summary>
    /// <param name="host">API base URL.</param>
    /// <param name="accessToken">Bearer token.</param>
    /// <param name="transportOptions">Optional HTTP transport configuration.</param>
    /// <returns>Configured client instance.</returns>
    public static Client WithToken(
        string host,
        string accessToken,
        TransportOptions? transportOptions = null
    )
    {
        return new Client(
            new BearerAuthenticator(host, accessToken),
            transportOptions ?? TransportOptions.Builder().Build()
        );
    }

    /// <inheritdoc/>
    public void Dispose()
    {
        _apiClient.Dispose();
    }
}
